# Core scientific packages
numpy==1.26.4
pandas

# Deep learning frameworks
tensorflow==2.15.0
torch
torchvision
torchaudio

# Bird detection and audio processing
birdnetlib==0.13.0
librosa==0.10.1
resampy==0.4.2
soundfile==0.12.1

# PyAudio (Windows users: install via pipwin, not pip)
# pip install pipwin && pipwin install pyaudio
pyaudio ; platform_system != 'Windows'

# FastAPI and server
fastapi==0.104.1
uvicorn==0.24.0
aiofiles
websockets==12.0
python-multipart==0.0.6
python-json-logger==2.0.7

# Flask compatibility
flask
flask-cors

# AI and ML tools
transformers
huggingface_hub[hf_xet]
google-generativeai

# Computer vision
opencv-python==4.8.1.78

# Database
sqlalchemy
